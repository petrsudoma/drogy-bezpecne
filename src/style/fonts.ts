import { Itim, Roboto, Roboto_Condensed } from 'next/font/google';

export const itim = Itim({
  weight: '400',
  subsets: ['latin'],
});

export const robotoCondensed = Roboto_Condensed({
  weight: ['400', '300'],
  subsets: ['latin'],
});

export const roboto = Roboto({
  weight: ['400', '300'],
  subsets: ['latin'],
});
